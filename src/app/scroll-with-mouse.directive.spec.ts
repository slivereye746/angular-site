import { ScrollWithMouseDirective } from './scroll-with-mouse.directive';

describe('ScrollWithMouseDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollWithMouseDirective();
    expect(directive).toBeTruthy();
  });
});
